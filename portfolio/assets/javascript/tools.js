var inputFile = process.argv[2];

var fs = require("fs");

fs.readFile()

// attempting to write a Node function that takes in input, writes to a file.  Re-visit when time permits.  // use-case: pulling in "min.css" files, etc. -- maybe "axios"?